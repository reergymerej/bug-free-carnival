language: elixir
elixir:
  - 1.9.4
env:
  - MIX_ENV=test
script:
  - mix deps.get
  - mix compile
  - mix test
after_success:
  - mix deps.get --only docs
  - MIX_ENV=docs mix inch.report
cache:
  directories:
    - _build
    - deps
